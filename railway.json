{
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "npm install",
      "watchPatterns": ["server/**"]
    },
    "deploy": {
      "startCommand": "node server.js",
      "healthcheckPath": "/"
    }
  }